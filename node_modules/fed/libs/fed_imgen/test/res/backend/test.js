module.exports = {
  "get /_/:settings": function(req, res) {
    var color, height, settings, size, width, _ref, _ref1;
    settings = req.param("settings");
    _ref = settings.split(/\|/), size = _ref[0], color = _ref[1];
    _ref1 = size.split(/x|X/), width = _ref1[0], height = _ref1[1];
    if (!height) {
      height = width;
    }
    return this.render.text("" + width + "," + height + "|" + color);
  }
};
