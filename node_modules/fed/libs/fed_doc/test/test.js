
/*
 *	Test fed_doc
 *	@author: ijse
 */
var fedDoc, fs, path, should;

fs = require("fs");

path = require("path");

should = require("chai").should();

fedDoc = require("../index.coffee");

describe("FED_DOC", function() {
  return it("Should get export html file", function() {
    var destFile, objList, srcFolder;
    srcFolder = "" + __dirname + "/res";
    destFile = "" + __dirname + "/testOut.html";
    objList = fedDoc.makeDoc(srcFolder, destFile);
    return path.existsSync(destFile).should.ok;
  });
});
