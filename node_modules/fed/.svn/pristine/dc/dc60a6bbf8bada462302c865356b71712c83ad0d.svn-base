/*
 * fedk
 * https://github.com/crossjs/fedk
 *
 * Copyright (c) 2014 crossjs
 * Licensed under the MIT license.
 */

module.exports = function(grunt) {

  grunt.initConfig({

    coffee: {
      options: {
        bare: true
      },
      t0: {
        files: {
          'fed.js': 'fed.coffee'
        }
      },
      t1: {
        expand: true,
        // flatten: true,
        cwd: 'libs',
        src: ['**/*.coffee'],
        dest: 'libs',
        ext: '.js'
      },
      t2: {
        expand: true,
        // flatten: true,
        cwd: 'plugins',
        src: ['**/*.coffee'],
        dest: 'libs',
        ext: '.js'
      }
    },

    clean: {
      t0: {
        src: ['**/*.coffee']
      }
    }

  });

  grunt.loadNpmTasks('grunt-contrib-coffee');
  grunt.loadNpmTasks('grunt-contrib-clean');

  grunt.registerTask('default', ['coffee']);

};
