
/*
	Generate Image File
 */
var imageUtil;

imageUtil = require("images");

exports.createImage = function(fileName, width, height, color) {
  var img;
  img = new imageUtil.Image(parseInt(width), parseInt(height));
  img.fill.apply(img, color);
  img.save(fileName);
  return fileName;
};
